export type HeaderProps = {
  children: React.ReactNode;
};

export type SizeType = {
  [key: string]: string;
  wide: string;
  desktop: string;
  tablet: string;
  phone: string;
};
export type InputType = {
  label: string;
  [x: string]: any;
};
